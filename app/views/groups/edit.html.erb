<h1>グループの編集</h1>
<p>このクイズグループを編集しましょう。</p>
<%= form_for @group , :html => { :class => "well form-inline" } do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <% if current_user %>
    <%= f.hidden_field :user_id, :value => current_user.id %>
  <% else %>
    <%= f.hidden_field :user_id %>
  <% end %>
  <%= f.label :title,  "グループ名" %>
  <%= f.text_field :title%>
  <%= f.button "編集", :class => "btn" %>
<% end %>

